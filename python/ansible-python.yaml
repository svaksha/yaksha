#!/usr/bin/env ansible-playbook
---
- name: PyPI deps with Pip3
#  hosts: all
#  strategy: debug
  pip3:
    name: pip3
    package: 
        name: "{{item}}" 
        state: latest
        extra_args: --upgrade
        update_cache: yes
        executable: pip3 
      sudo: true
      with_items:
      - NumPy
      - tensorflow 
      - ['pytorch, 'pandas']    
      # For TESTING Ansible
      #- pip3 install molecule
      #- pip3 install 'molecule[docker]'
      # Or use ansible-lint
   #   - ansible-lint
   #   - molecule
      ##------------------------
      #requirements: yaksha/python/requirements.txt
      #https://github.com/deeptools/pyBigWig
      #- pyBigWig
...
